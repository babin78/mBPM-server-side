var router = require('express').Router();
var workitemCtrl=require('./workitem.controller')
/*
router.get('/',workitem.getAll)
router.get('/:id',workitem.getOne)
router.get('/process/:id',workitem.getByProcessID)
router.post('/adduser',workitem.addUser)
router.post('/removeuser',workitem.removeUser)
*/

module.exports = router;
